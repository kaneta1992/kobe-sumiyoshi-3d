# 契約06: ポップなキャラクター＆アニメーション（+車のポップ化）

ダイヤル: Opus向けゴール委譲

## 目的

- 要求: 操作キャラクターを「ポップ」なキャラクターに置き換え、アニメーションもポップにする（ユーザー明示要望 2026-08-13）。車も同じトーンに揃える。
- ゴール: 現在の箱組みアバターは町の品質に対して見劣りする。愛嬌のあるキャラクターが坂道を駆け回る画で、アプリの「顔」を作る。
- 成功条件: 地上視点のスクリーンショットで、キャラクターが生き生きと見える（静止画でもポーズに表情がある）。歩く/走る/乗るの動きが楽しい。マルチプレイの遠隔プレイヤーにも同じ品質で適用される。

## コンテキスト

- 既存実装: C:\Users\kanet\nine\src\game\avatar.ts（現在の箱組み人型・車。setColorでピア色対応）、src/game/character.ts（移動状態）、src/net/remote-players.ts（遠隔表示・8スロット再利用）
- 必ず先に読む: C:\Users\kanet\nine\.claude\skills\kobe-sumiyoshi-3d-dev\SKILL.md（落とし穴集）

## 実装ガイド（設計は委ねるが、以下は決定事項）

- **スタイル: 2.5〜3頭身のチビ/デフォルメ体型**。丸み優先（球・カプセル・角丸ベース）、明るくポップな配色。頭・目は大きく（目はシンプルで可愛く — テクスチャ or 小ジオメトリ）。ピアID由来の色替え（服・帽子等）は維持
- 実装方式は委ねる（プロシージャルなボーン階層+コードアニメ or SkinnedMesh+AnimationClip）。**外部アセットのダウンロードは禁止**（すべて生成。Blenderは未インストール — 導入せず手続き的に作ること）
- **アニメーション（ポップの核。すべて必須）**:
  - idle: 呼吸・体の揺れ・時々まばたき/首かしげ
  - walk: 腕振り+足運び+上下バウンス
  - run: 前傾・大きなストライド・**スクワッシュ&ストレッチ**・着地の弾み
  - jump/落下があるなら滞空ポーズ（なければ省略可）
  - 乗車: 車内に座り、ハンドルに手。降車時は小さくジャンプして降りる等の遊び
  - 遷移はブレンド（カクッと切り替えない）。坂道では体を斜面に応じて傾ける
- **車のポップ化**: 丸みのある車体（角丸・キャビン強調）、ホイール回転・ステア連動・加減速での車体ピッチ/ロール（サスペンション演出）、ブレーキランプ点灯
- 遠隔プレイヤー: 同じキャラ・アニメを state（mode/speed）から駆動（**追加の同期項目は増やさない**。増やす場合は後方互換を保ち notes で報告）
- 性能: mobile draw call 予算（≤100）内。キャラのポリゴン数は控えめに（LOD不要の軽さが理想）。フレーム内アロケーションゼロ規律を維持

## エッジケース

- E34: アニメ状態機（idle/walk/run/乗車/降車）の遷移で腕・足が瞬間ワープしない（ブレンド）
- E35: 急坂での接地 — 足が地形にめり込む/浮くのを傾き補正で軽減（完全IKまでは不要）
- E36: 遠隔プレイヤーのアニメが補間位置の速度から自然に駆動される（速度0でwalkし続けない）
- E37: 8スロット再利用（remote-players）でキャラ差し替え後も色替え・表示/非表示が正しく動く
- E38: 影 — キャラに影を落とし、影予算（mobile）を壊さない

## スコープと負の制約

- 変更してよい: src/**, index.html, package.json
- 変更禁止: orchestration-log.md, docs/**, .claude/**, .git/**, tools/**, public/data/**
- git commit は行わない / 依存追加なし（three内で完結） / Math.random() 禁止 / 外部アセットDL禁止
- 世界側（world/）の変更は最小限（キャラと無関係な変更をしない）

## 合格基準

実行シェル: bash

- `npx tsc --noEmit` → exit 0 / `npm run build` → exit 0
- `grep -RIn "Math.random" src/ | grep -v "// allow"; test $? -eq 1` → exit 0
- mobileプリセットの draw call 実測を報告（≤100維持）
- **目視イテレーション**: メインがスクリーンショットでレビューしフィードバックを返す。最低1回の修正ラウンドを想定しておくこと（報告後もSendMessageで継続指示が来る）

## 出力契約

日本語・20行以内: status / files_changed / commands_run(+draw実測) / summary（キャラ構成・アニメ一覧・追補E） / notes

## エスカレーション条件

- three/webgpu の SkinnedMesh/アニメ機能に致命的制約があり方式変更が必要 → 代替（ボーン階層+コードアニメ）へ自分で切り替えてよい（blocked不要、DECISIONとして報告）
- 予算内に収まらない → 実測値と削減案を添えて blocked
